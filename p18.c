#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char *argv[]) {
int wh = atoi(argv[1]);
int sph = atoi(argv[2]);
printf("%s, %d\n", "Dinero total:", wh*sph);

  return 0;
}
